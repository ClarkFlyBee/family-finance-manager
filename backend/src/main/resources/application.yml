server:
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl   # 控制台打印 SQL
    map-underscore-to-camel-case: true  # 弃用驼峰命名自动映射
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.wcw.backend.entity

logging:
  level:
    com.wcw.backend.Mapper: debug

zhipu:
  api-key: 18bc7ebf1ce34c659b5c2290f09dd6b6.UDA5qT2pBkE48EQ6